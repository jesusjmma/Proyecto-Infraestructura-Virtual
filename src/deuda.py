from dataclasses import dataclass

@dataclass
class deuda:
    '''
    Objeto que representa la deuda que tiene un usuario frente a otro.
    Consiste de un par de identificadores de usuario (quien recibe y
    quien paga), además del concepto, importe, y un atributo que comprueba
    si se ha pagado la deuda o no
    '''
    pass